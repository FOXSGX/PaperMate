# 前端开发端口（默认 5173，需与后端 CORS 一致）
VITE_PORT=5173

# 后端地址：仅用于 Vite 开发/预览代理，浏览器仍请求同源 /api
VITE_BACKEND_URL=http://127.0.0.1:8000

# 可选：直连后端 API 时设置，例如 http://127.0.0.1:8000/api
# 默认留空，使用相对路径 /api（经 Vite 代理）
# VITE_API_BASE=/api
